# 最终种子清单

本目录只保存经过确定性筛选、需要进入版本控制的最终种子场景清单。

- `formal_candidates.csv`：从34类正式技能的种子候选池中确定性筛选得到的5,000个唯一场景，共保留33,914条完整正式技能标签，覆盖34/34类正式技能。5类零命中候选规则不进入本清单。
- 开发扫描和正式扫描的原始候选池属于可再生成产物，统一保存在被Git忽略的 `outputs/seed_detection/`。
